name = "kritzbitz-frontend"
compatibility_date = "2025-01-01"
pages_build_output_dir = "out"

# NOTE: NEXT_PUBLIC_* variables must be set in Cloudflare Pages dashboard
# (Settings > Environment variables) - they are baked in at build time.
# The [vars] section here only applies to Workers runtime, not static builds.
